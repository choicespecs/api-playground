# Documentation Pipeline Run — 2026-05-28T00-00-00

## Run metadata

| Field | Value |
|---|---|
| Date | 2026-05-28 |
| Project | api-playground |
| Project root | /Users/christopherlee/api-playground |
| Pipeline model | claude-sonnet-4-6 |

---

## Discovery results

### Project identity

- **Name:** api-playground
- **Primary language:** Go (1.26.3 module)
- **Frontend:** HTMX 1.9 + DaisyUI 4 + Tailwind (CDN-loaded)
- **Desktop framework:** webview_go (WKWebView on macOS)
- **Web framework:** Gin v1.12.0

### Source files processed

| File | Role |
|---|---|
| `main.go` | Entry point, window bootstrap, route registration |
| `handlers.go` | POST /send proxy engine |
| `auth.go` | Token API auth profiles |
| `collections.go` | Collections CRUD + saved requests + collection variables |
| `variables.go` | Global variables + expansion engine |
| `history.go` | History persistence |
| `curl_parser.go` | curl tokeniser + raw HTTP parser |
| `environments.go` | Environments (dormant dead code) |
| `screen_darwin.go` | CGO CoreGraphics screen sizing |

### Templates processed

`index.html`, `form.html`, `response.html`, `history.html`, `collections_panel.html`, `collection_settings.html`, `collection_options.html`, `variables.html`, `auth_profiles.html`, `auth_options.html`

### Existing docs at pipeline start

- `README.md` — existed, contained ASCII art layout diagram and complete API route table
- `docs/` directory — existed but was empty (only `docs/context/` subdirectory)
- No `CLAUDE.md` existed

---

## Phase results

### Phase 2 — Architecture (docs/ARCHITECTURE.md)

**Sections written:**
- High-level component map (Mermaid flowchart TD with subgraphs)
- Runtime startup sequence (Mermaid sequenceDiagram)
- Request proxy flow POST /send (Mermaid flowchart TD)
- Variable expansion architecture (Mermaid flowchart TD)
- Auth profile token lifecycle (Mermaid stateDiagram-v2)
- Data model relationships (Mermaid erDiagram)
- Template rendering model (table + prose)
- File locations at runtime (table)
- Key design constraints (prose)

**Components documented:** main, handlers, auth, collections, variables, history, curl_parser, screen_darwin, environments, all 10 templates, 4 JSON data files, webview/WKWebView layer

**Integrations documented:** External API proxy, CoreGraphics CGO, HTMX event bus

### Phase 3 — Flow (docs/FLOW.md)

**Flows documented:**
1. Sending an HTTP request (full sequenceDiagram with auth injection + 401 retry)
2. curl / raw HTTP import (sequenceDiagram with auto-detection)
3. Variable resolution (flowchart TD, client-side preview vs server-side authoritative paths)
4. Auth token acquisition and injection (sequenceDiagram with cache hit/miss)
5. Collection request save / load (sequenceDiagram)
6. History persistence (flowchart TD with all CRUD operations)
7. Panel resize drag (flowchart TD)
8. HTMX event bus (flowchart TD mapping all HX-Trigger producers to consumers)

**Data models documented:** HistoryEntry, AuthProfile, Collection, SavedRequest, CollVar, Variable, KVPair

### Phase 4 — Design (docs/DESIGN.md)

**Decisions documented:** 12 design decisions recorded with rationale and trade-offs:
1. Hypermedia-first architecture (HTMX over SPA)
2. Server-side variable expansion
3. ID generation via time.Now().UnixNano()
4. File-per-concern JSON persistence (no database)
5. Token API as the only auth profile type
6. Token caching with 30-second buffer and 401 auto-retry
7. 50 KB response body cap
8. CGO for screen sizing
9. Paste detection in the URL bar
10. No authentication on the local HTTP server
11. Template loading from current working directory
12. Collection variables override global variables

**Tech debt items flagged:**
- `environments.go` is dead code (handlers not registered in main.go)
- Client-side JS regex duplicates server-side expansion logic
- No write batching or file locking on JSON persistence

### Phase 5 — Security (docs/SECURITY.md)

**Auth mechanisms documented:** Token API profile flow (3-stage: login, extract, inject), variable-based static credentials

**Input entry points documented:** 8 entry points (URL, headers, params, body, curl import, raw HTTP import, history import, auth profile login body)

**Security issues by severity:**

| Severity | Issue |
|---|---|
| Medium | Token exfiltration via GET /variables/map (tokens in variables are JSON-readable by any local process) |
| Medium | Proxy abuse via POST /send (any local process can use configured auth profiles) |
| Medium | auth_profiles.json stores cached tokens in plaintext |
| Low–Medium | History export contains sensitive URLs and bodies |
| Low | No CSRF or origin checking on local server |
| Low | Auth profile metadata (login URL, config) readable via GET /auth-profiles |

**Hardcoded secrets found:** 0

### Phase 6 — Inline documentation

**Files updated with doc comments:**
- `main.go` — added package-level comments for `main()` and `startServer()`
- `handlers.go` — added numbered processing stages (1–13) to `SendRequest()`
- `auth.go` — added struct-level AuthProfile doc, function docs for all exported and unexported functions
- `variables.go` — added type docs, regex explanation, function docs for all functions
- `history.go` — added type docs with field descriptions, method docs, function docs for all functions
- `curl_parser.go` — added type docs, tokenize() with example, parseCurl() with supported flags list, parseRawHTTP() with target form examples
- `screen_darwin.go` — added CGO function comment and Go function doc

**Files skipped:** `environments.go` (dormant dead code — existing comments accurate), `collections.go` (existing comments adequate, handlers already have doc comments)

**Functions documented:** ~45 across 7 files

### Phase 7 — Meta files

**CLAUDE.md:** Created new. Sections: project summary, build/run commands, project structure, 6 key architecture rules, common task guides (add route, add template, add data type, modify variable expansion), data file reference table, known issues/tech debt, documentation index.

**README.md:** Rewrote. Replaced ASCII art layout diagram with Mermaid flowchart. Retained all existing content (requirements, install options, feature table, variables, collections, auth profiles, project layout, API routes table, data files). Added curl export feature to feature table. Added docs/ to project layout section.

---

## Warnings and issues encountered

1. `environments.go` implements a complete Environment/EnvVar/AppSettings system (handlers: `EnvSelectorHandler`, `EnvironmentsPanelHandler`, `EnvironmentCreateHandler`, `EnvironmentDeleteHandler`, `EnvironmentActivateHandler`, `EnvironmentDeactivateHandler`, `EnvVariableCreateHandler`, `EnvVariableDeleteHandler`) but none of these routes are registered in `main.go`. The file is dead code. It was left in place and noted in DESIGN.md, SECURITY.md, CLAUDE.md, and README.md.

2. `collections.go` already had good inline comments on all HTTP handlers. No modifications were made to avoid regressing existing documentation quality.

3. The `go.mod` specifies `go 1.26.3` which is a future version (current stable as of knowledge cutoff is 1.23.x). This is noted but not modified.

4. `auth_profiles.json` is referenced in `auth.go` as the `authFile` constant but the file is listed in `.gitignore` (indirectly, along with all `*.json` data files). This is correct — data files should not be version controlled.

---

## Files created or modified

### Created
- `/Users/christopherlee/api-playground/CLAUDE.md`
- `/Users/christopherlee/api-playground/docs/ARCHITECTURE.md`
- `/Users/christopherlee/api-playground/docs/FLOW.md`
- `/Users/christopherlee/api-playground/docs/DESIGN.md`
- `/Users/christopherlee/api-playground/docs/SECURITY.md`
- `/Users/christopherlee/api-playground/docs/context/2026-05-28T00-00-00.md` (this file)

### Modified
- `/Users/christopherlee/api-playground/README.md` — replaced ASCII art diagram with Mermaid; added curl export feature; added docs/ to layout; minor wording improvements
- `/Users/christopherlee/api-playground/main.go` — added package-level doc comments
- `/Users/christopherlee/api-playground/handlers.go` — added numbered processing stage comments to SendRequest
- `/Users/christopherlee/api-playground/auth.go` — added struct and function doc comments throughout
- `/Users/christopherlee/api-playground/variables.go` — added type, regex, and function doc comments throughout
- `/Users/christopherlee/api-playground/history.go` — added type, method, and function doc comments throughout
- `/Users/christopherlee/api-playground/curl_parser.go` — added type, tokenize example, parseCurl flag table, and function doc comments throughout
- `/Users/christopherlee/api-playground/screen_darwin.go` — added CGO C function comment and Go function doc comment

### Unchanged
- `collections.go` — existing comments adequate
- `environments.go` — dead code, existing comments accurate
- All files in `templates/` — HTML templates not modified
- `build-app.sh` — shell script not modified
- `go.mod`, `go.sum` — not modified
